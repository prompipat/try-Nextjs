"use client";

export default function AddToCart() {
  return (
    <div>
      <button
        className="btn btn-primary"
        onClick={() => console.log("clicked")}
      >
        Add to Cart
      </button>
    </div>
  );
}
